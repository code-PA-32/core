{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "start:fe": "turbo run dev --filter=app",
    "start:be": "turbo run dev --filter=server"
  },
  "name": "core",
  "dependencies": {
    "turbo": "latest"
  },
  "packageManager": "bun@1.0.7",
  "workspaces": [
    "fe/app",
    "be/app",
    "packages/*",
    "configs/*"
  ]
}
